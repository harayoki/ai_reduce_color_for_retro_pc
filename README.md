# ai_reduce_color_for_retro_pc

MSX やレトロ PC の色数・ドット数制約に近づけるための画像変換ツールキットです。`spec.md` にある設計メモをもとに、
Python パッケージ `msxfilter` で色味変換（パレット寄せ）とドット寄せの 2 段処理を行える最小構成をまとめています。
将来的に Gradio UI で操作できる公開を想定しつつ、現状は CLI とコアロジックの実装・検証を進めるための下地を置いています。
最後に、とりあえずファイルをそろえた状態、まだ動かしていない、実装はこれから。
